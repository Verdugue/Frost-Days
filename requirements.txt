# Coeur calcul / données
pandas>=2.0
numpy>=1.24
scipy>=1.10          # cKDTree pour la recherche de stations
pyarrow>=12.0        # lecture/écriture parquet (cache)
requests>=2.28       # téléchargement des données open data

# Visualisation / interface
plotly>=5.15
streamlit>=1.30

# Notebooks (statistiques descriptives)
matplotlib>=3.7
jupyter

# Développement / tests
pytest>=7.0
